import React from 'react';
import ReactDOM from 'react-dom/client';
import SovereignBrain from './SovereignBrain';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SovereignBrain />
  </React.StrictMode>
);
